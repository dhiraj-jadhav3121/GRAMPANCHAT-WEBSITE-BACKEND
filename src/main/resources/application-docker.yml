spring:
  application:
    name: GRAMPANCHAT-WEBSITE

  datasource:
    url: jdbc:mysql://host.docker.internal:3306/gram_panchayat
    username: root
    password: Amol@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: dhirajj8048@gmail.com
    password: aqjf ufve jady thxi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

server:
  port: 8080